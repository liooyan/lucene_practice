package cn.lioyan.search;

/**
 * {@link org.apache.lucene.search.TwoPhaseIterator}
 * 二阶段的 Iterator
 *
 *
 *
 *
 * 也可以获取为普通的Iterator。
 * 定义了{@link org.apache.lucene.search.TwoPhaseIterator#matches()} 方法，用于判断当前文档是否匹配。
 * 通过 asDocIdSetIterator 获取的 Iterator，只返回符合要求的 matches 的文档。
 * @author com.lioyan
 * @since 2023/7/24  10:00
 */
public class TwoPhaseIterator
{
}
